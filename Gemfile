source "https://rubygems.org"

# Fastlane handles building, signing, and TestFlight upload
gem "fastlane", "~> 2.220"

# CocoaPods — installed via gem so CI gets exact same version as local
gem "cocoapods", "~> 1.14"
